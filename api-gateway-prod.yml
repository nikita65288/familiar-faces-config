eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka/
  instance:
    hostname: api-gateway
    preferIpAddress: false      
    
server:
  tomcat:
    accesslog:
      enabled: true
      
jwt:
  secret: ${JWT_SECRET}
